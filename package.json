{
  "name": "pwa-weather-app",
  "version": "1.0.0",
  "description": "A PWA weather app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "grunt",
    "serve": "npm run build && firebase serve",
    "deploy": "firebase deploy",
    "predeploy": "npm run build",
    "dev": "npx http-server -a 127.0.0.1 -p 5050 --cors"
  },
  "keywords": [
    "weather",
    "pwa"
  ],
  "author": "David Okanlawon",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "grunt": "^1.3.0",
    "grunt-cli": "^1.3.2",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-cssmin": "^3.0.0",
    "grunt-inline": "^0.3.7",
    "grunt-processhtml": "^0.4.2",
    "grunt-purgecss": "^2.3.0",
    "grunt-terser": "^1.0.0",
    "grunt-uncss": "^0.9.1",
    "http-server": "^0.12.3",
    "matchdep": "^2.0.0"
  }
}
